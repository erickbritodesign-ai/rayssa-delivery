name: rayssa_core
description: Modelos, enums e utilitários compartilhados do Rayssa Delivery.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  cloud_firestore: ^5.6.0
  equatable: ^2.0.7

dev_dependencies:
  flutter_lints: ^5.0.0
